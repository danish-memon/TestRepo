// Import the LightningElement base class from the LWC framework.
// This provides core functionality for creating Lightning Web Components.
import { LightningElement } from 'lwc';

// Export the component class so it can be used in the Lightning app.
// The class extends LightningElement, making it an LWC component.
export default class MyLwcComponent extends LightningElement {
    // Component logic will go here (properties, methods, getters, etc.)
}
